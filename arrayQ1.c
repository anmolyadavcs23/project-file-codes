//write a program to find sum, average, % of a marks of a student in the form of array
#include<stdio.h>
#include<conic.h>
{
	int i,n,sum=0;
	float avg,per;
	printf("enter the size of array: ");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	 printf("enter the %d marks :n\n",i);
	 scanf("%d",&a[i]); 
}
for(i=0;i<n;i++)
{
	print
}

